import Styles from './style.module.css';

const MagazineDetails = ({slug = []}: {slug?: string[]}) => {
    return (
        <div className={Styles.single_page}>
details
        </div>
    )
}

export default MagazineDetails
